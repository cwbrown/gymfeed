<%= simple_form_for @score, html: { class: 'form-horizontal' } do |f| %>

  <% if @score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@score.errors.count, "error") %> prohibited this score from being saved:</h2>

      <ul>
      <% @score.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.input :user, collection: User.all, :label_method => :name, :value_method => :id %>
    <%= f.input :value %>
    <%= f.input :scaled, as: :boolean %>
    <%= f.input :personal_record, as: :boolean %>
    <%= f.input :completed, as: :datetime %>
    <%= f.button :submit %>
<% end %>
